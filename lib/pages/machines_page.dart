import 'package:flutter/material.dart';

class MachinesPage extends StatelessWidget {
  const MachinesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Page Machines", style: TextStyle(fontSize: 24)),
    );
  }
}
